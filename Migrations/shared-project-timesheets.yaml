# --------------------------------------------------------------------------------------------------
# Generate a new migration with:
# bin/console doctrine:migrations:diff --em=default --configuration=var/plugins/SharedProjectTimesheetsBundle/Migrations/shared-project-timesheets.yaml
#
# Execute all migrations with:
# bin/console kimai:bundle:shared-project-timesheets:install
# --------------------------------------------------------------------------------------------------

table_storage:
  table_name: bundle_migration_shared_project_timesheets
migrations_paths:
  KimaiPlugin\SharedProjectTimesheetsBundle\Migrations: var/plugins/SharedProjectTimesheetsBundle/Migrations
